# Flutter — point staff app at live Hostinger API
# Temporary domain: api.progarage.cloud (future: api.progarageos.com)

API_BASE_URL=https://api.progarage.cloud/api
APP_ENV=production

# Local Docker (optional offline dev)
# API_BASE_URL=http://192.168.x.x:8000/api
# APP_ENV=local
