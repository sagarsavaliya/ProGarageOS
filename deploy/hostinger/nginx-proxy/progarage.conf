# HTTPS proxy for Pro Garage OS API
# Domain: api.progarage.cloud (temporary — see deploy/hostinger/domain.env)

server {
    listen 80;
    server_name api.progarage.cloud;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name api.progarage.cloud;

    ssl_certificate     /etc/letsencrypt/live/api.progarage.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.progarage.cloud/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    client_max_body_size 64M;

    resolver 127.0.0.11 valid=30s ipv6=off;

    location / {
        set $upstream progarage_nginx;
        proxy_pass         http://$upstream:80;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }
}
