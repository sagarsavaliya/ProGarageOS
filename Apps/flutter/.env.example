# Flutter — point staff app at live Hostinger API
# Physical device / emulator: use HTTPS domain once DNS + SSL are ready

# Production (Hostinger KVM2)
API_BASE_URL=https://api.progarageos.com/api
APP_ENV=production

# Temporary HTTP testing (before SSL)
# API_BASE_URL=http://69.62.78.240/api

# Local Docker (deprecated for daily dev)
# API_BASE_URL=http://192.168.x.x:8000/api
# APP_ENV=local
